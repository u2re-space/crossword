/**
 * Views Module
 * 
 * Central export point for all view components.
 * Views are shell-agnostic content components that can be
 * loaded into any shell.
 */

// View type exports
export * from "./types";

// Individual view exports (for direct imports)
// Each view is also registered in the ViewRegistry for lazy loading
