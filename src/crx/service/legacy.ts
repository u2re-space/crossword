/**
 * Legacy test code — retained for reference only.
 * All functionality has been consolidated into api.ts and sw.ts.
 * @deprecated
 */
export const enableLegacyTestCode = (_chrome: typeof chrome) => {
    // No-op: legacy handlers removed in CRX remake.
    // Context menu and recognition logic is now in sw.ts + api.ts.
};
